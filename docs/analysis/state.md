# Состояние исследования

Обновлять вместе с кодом. Один экран: что проверяется сейчас, что уже закрыто.

**Последний срез live:** 2026-08-30, label `до фикса: 95 сделок на сломанном филе`
(95 сделок, −42.8R, expectancy −0.45R, WR 26%). См. `data/analysis/metrics.csv`.

**Backtest после фикса:** +0.441R на 1655 сделках с издержками 1 б.п./нога,
2024-07-04 → 2026-08-08 (было +0.244R на 2368 без издержек). Подробности и разбивка по слотам — [`../baseline.md`](../baseline.md).

---

## Сейчас в работе

| Гипотеза | Статус | Как проверяется |
|----------|--------|-----------------|
| Фикс входа убирает мёртвые сделки | **подтверждено в backtest, ждёт live** | out-of-sample 2024-07→2026-08: exp_R +0.244 → +0.560, WR 51.8% → 64.1%, DD −38% на 1728 сделках. В live проверять `dead_on_arrival_share` → 0, `same_bar_share` < 0.2 |
| Трейлинг без фикс. тейка лучше TP 1.65R | **ждёт данных** | ORC-чемпионы с `take_profit_enabled: false`; сравнить `sum_r` со срезом «до фикса» |
| `min_stop_bps: 20` не съедает слишком много сигналов | **ждёт данных** | число сделок на прогон vs предыдущий срез; на старых 95 порог 30 оставлял лишь 28 из 84 ORC-сигналов, поэтому взят порог по издержкам (~10x round-trip), а не по медиане |

## Закрыто

| Вывод | Разбор |
|-------|--------|
| Ретест-лимит ORC давал отрицательный отбор входов; 38 сделок из 95 на −31.5R помечены аудитом по кодам качества входа | [0001](0001-entry-fill-adverse-selection.md) |
| Фиксированный тейк 1.65R рубит победителей: 18/18 выходов продолжили движение, медиана недобора +2.79R | [0001](0001-entry-fill-adverse-selection.md) |
| Частичная фиксация 50% на 1R поднимает WR 24% → 53%, но снижает итог (+6.4R против +16.9R у чистого трейла) | [0001](0001-entry-fill-adverse-selection.md) |
| Никакая политика выхода не спасает на сломанных филах — выход не был связывающим ограничением | [0001](0001-entry-fill-adverse-selection.md) |

| Тиковая детализация ломает результат backtest | **отвергнуто** | стресс `-intrabar-oscillations 2/3`: +0.560R → +0.551R, при n=3 без изменений. Медиана диапазона M5 ≈ 18.5 б.п. против R ≥ 20 б.п. — откат в 1R редко умещается в свечу |

## Долги

- **Переоптимизация чемпионов.** Текущие параметры подобраны на сломанной механике фила;
  числа baseline получены на неоптимальных параметрах.
- ~~Нет модели проскальзывания~~ — **сделано**: `costs.slippage_bps`, по умолчанию
  1 б.п. на ногу. Чувствительность ≈ −0.10R на каждый б.п. Открытый вопрос: какое
  значение реалистично для наших тикеров — уточнить по фактическим филам live.
- **Лаг свечного фида в live.** На старых сделках `entry_bar_close` совпадал с закрытием
  ПРЕДЫДУЩЕГО бара в 40 случаях из 95. Backtest такой дефект не воспроизводит в принципе.
- **Разрыв backtest / live не измерен заново.** На окне 2026-08 backtest давал +0.236R
  против −0.45R в live. Гейт теперь общий для обоих путей; сократился ли разрыв —
  покажет только новый virtual-прогон.
- **Мало данных.** 95 сделок за 3 недели одного режима. Механику видно, edge — нет.
  До ~200 сделок выводы про expectancy не делать.
