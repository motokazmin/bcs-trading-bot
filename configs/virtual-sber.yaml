# Paper trading: один тикер SBER (акции MOEX).
# Запуск: go run ./cmd/bot -config configs/virtual-sber.yaml

trading_mode: virtual

tickers:
  - SBER

class_code: TQBR
candle_timeframe: M5

risk:
  deposit: 100000
  max_daily_loss_percent: 2
  risk_per_trade_percent: 0.5

strategy:
  lookback: 20

virtual:
  balance: 100000

session:
  timezone: Europe/Moscow
  eod_close_time: "18:40"      # за 10 мин до закрытия основной сессии TQBR
  session_open_time: "10:00"
